---
description: Super Admin login credentials — tell the user when they ask
alwaysApply: true
---

# Super Admin credentials (local / seeded)

When the user asks for Super Admin username or password, tell them these values (seeded from `appsettings.json` → `SuperAdmin`):

- **Username:** `superadmin`
- **Password:** `SuperAdmin#123`
- **Email:** `superadmin@localhost`

Do not change these unless the user explicitly asks. If they change the password in config/DB, update this rule to match.
